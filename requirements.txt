# Interfaz gráfica (siempre necesario)
PySide6>=6.5

# Núcleo (siempre necesario)
pymupdf>=1.24
ezdxf>=1.1
shapely>=2.0
openpyxl>=3.1        # lectura de columna TEXTO/TEXTOS desde Excel

# Ruta rasterizada (solo si procesas escaneos / PDFs imagen)
opencv-python>=4.8
numpy>=1.24
pillow>=10.0

# Vista previa opcional
matplotlib>=3.7

# Georreferenciación (opcional) — menú "Herramientas → Georreferenciar…"
# Calza el plano contra las calles reales de Los Ángeles (NavigateLA) da
# coordenadas de trazado/anteproyecto, NO grado construcción; el dato
# topográfico real viene del levantamiento/Excel.
pyproj>=3.6              # reproyección lon/lat <-> State Plane (EPSG:2229)
scikit-image>=0.22       # ajuste de la transformación por puntos de control
contextily>=1.5          # opcional: imagen base (satélite/calles) detrás de las centerlines

# Desarrollo / pruebas (opcional) — corre las pruebas de humo con:  pytest
pytest>=8.0