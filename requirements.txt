# Interfaz gráfica (siempre necesario)
PySide6>=6.5

# Núcleo (siempre necesario)
pymupdf>=1.24
ezdxf>=1.1
shapely>=2.0
openpyxl>=3.1        # lectura de columna TEXTO/TEXTOS desde Excel

# Ruta rasterizada (solo si procesas escaneos / PDFs imagen)
opencv-python>=4.8
numpy>=1.24
pytesseract>=0.3.10   # requiere binario Tesseract-OCR instalado en el sistema
pillow>=10.0

# ICR manuscrita (opcional, offline) — solo si activas el checkbox "ICR manuscrita"
easyocr>=1.7          # la primera ejecución descarga el modelo (~100 MB)

# Vista previa opcional
matplotlib>=3.7

# Georreferenciación (opcional) — menú "Georreferencia → Georreferenciar…"
# Calzar sobre imagen satelital da coordenadas UTM APROXIMADAS (trazado/anteproyecto),
# NO grado construcción; el dato topográfico real viene del levantamiento/Excel.
PySide6-WebEngine>=6.5   # mapa Leaflet embebido (satélite Esri / calles OSM)
pyproj>=3.6              # reproyección lon/lat <-> UTM
scikit-image>=0.22       # ajuste de la transformación por puntos de control